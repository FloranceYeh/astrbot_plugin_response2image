{
  "base_url": {
    "description": "Base URL",
    "type": "string",
    "hint": "For example: https://api.openai.com",
    "default": ""
  },
  "api_key": {
    "description": "API Key",
    "type": "string",
    "hint": "API key for the image generation service",
    "default": ""
  },
  "model": {
    "description": "Model ID",
    "type": "string",
    "hint": "For example: gpt-5.3-codex (the model must be able to use the image_generation tool)",
    "default": ""
  },
  "timeout_seconds": {
    "description": "Request timeout in seconds",
    "type": "int",
    "default": 200
  },
  "reference_prompt_general": {
    "description": "General reference-image prompt",
    "type": "text",
    "hint": "Used when a task contains reference images and is not in edit/selfie mode. Supports multiple lines; each non-empty line becomes one instruction.",
    "default": "参考图片仅作为视觉依据；以用户原始需求为准，只沿用用户要求保留的主体、风格、构图或关键元素。"
  },
  "reference_prompt_edit": {
    "description": "Edit-mode reference-image prompt",
    "type": "text",
    "hint": "Used for aiedit or auto mode with reference images. Supports multiple lines; each non-empty line becomes one instruction.",
    "default": "只修改用户明确要求修改的内容；未提及的主体身份、数量、构图、比例、姿态、背景关系和关键细节尽量保持不变。"
  },
  "reference_prompt_selfie": {
    "description": "Selfie-mode reference-image prompt",
    "type": "text",
    "hint": "Used for selfie mode. Supports multiple lines; each non-empty line becomes one instruction.",
    "default": "参考图片仅作为人物与外观依据；优先保持人物身份、脸部特征和整体一致性，根据用户要求生成自然的自拍照片效果。"
  },
  "selfie_reference_images": {
    "description": "Selfie reference images",
    "type": "file",
    "hint": "Upload one or more reference images in WebUI. They will be merged with images saved by the selfie_ref set command.",
    "default": []
  }
}
